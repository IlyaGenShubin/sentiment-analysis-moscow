# Анализ тональности отзывов горожан Москвы

> **Автоматизированный сервис для анализа эмоциональной окраски русскоязычных текстов**

Отзывы горожан — ценная информация, которая помогает столице развивать городские цифровые сервисы.  
Ручной анализ большого объёма комментариев — трудоёмкая задача.  
Данное решение автоматически классифицирует тональность и позволяет экспертам **вручную корректировать результаты** через веб-интерфейс.

---

## 🎯 Возможности

- 📤 Загрузка CSV с отзывами (колонка `text`)
- 🤖 Классификация на 3 класса:  
  `0` — Отрицательная,  
  `1` — Нейтральная,  
  `2` — Положительная
- ✏️ **Ручная корректировка** через интерактивную таблицу
- 🔍 Поиск по тексту, фильтрация по источнику (`src`) и метке
- 📊 Визуализация распределения тональности
- 📥 Экспорт исправленных данных в CSV
- 📏 Оценка качества по **macro-F1** (при наличии экспертной разметки)

---

## 🛠️ Технологии

- **Модель**: cointegrated/rubert-tiny2
- **Backend**: FastAPI + PyTorch + Transformers (с поддержкой Safetensors)
- **Frontend**: Streamlit
- **Развёртывание**: Docker Compose
- **Поддержка**: macOS (MPS), Linux (CUDA/CPU)

---

## 🚀 Быстрый старт

### 1. Требования
- Docker и Docker Desktop (macOS)
- Обученная модель в формате Hugging Face Transformers (с файлом `model.safetensors`)

### 2. Подготовка модели
1. Обучите модель на трёхклассовой задаче.
2. Сохраните её:
   ```python
   trainer.save_model('./model')
   tokenizer.save_pretrained('./model')
   ```
3. Убедитесь, что в папке `model/` есть:
   - `config.json`
   - `model.safetensors`
   - `tokenizer.json`
   - `vocab.txt`
   - `special_tokens_map.json`
   - `tokenizer_config.json`
4. Поместите папку `model/` в корень проекта.


## 📂 Структура проекта

```
sentiment-analysis-moscow/
├── README.md
├── docker-compose.yml
├── backend/
│   ├── Dockerfile
│   ├── requirements.txt
│   ├── app.py
│   └── model.py
├── frontend/
│   ├── Dockerfile
│   ├── requirements.txt
│   └── app.py
└── model/
    ├── config.json
    ├── model.safetensors          # ← основной файл весов
    ├── special_tokens_map.json
    ├── tokenizer_config.json
    ├── tokenizer.json
    ├── training_args.bin
    └── vocab.txt
```

> 💡 Папка `model/` **не входит в репозиторий** (добавьте в `.gitignore`).

---

## 🚀 Запуск проекта

### Шаг 1: Установите зависимости и запустите контейнеры

```bash
# Остановите старые контейнеры
docker-compose down

# Соберите и запустите всё
docker-compose up --build
```

### Шаг 2: Дождитесь полной загрузки модели

В терминале вы увидите:

```
backend-1  | 🔄 Загрузка модели при старте приложения...
backend-1  | ✅ Модель успешно загружена!
backend-1  | INFO:     Uvicorn running on http://0.0.0.0:8000
```

> ⏱️ Первый запрос к `/predict` может занять до 2–3 минут для файла из 58k строк — это нормально.

### Шаг 3: Используйте веб-интерфейс

- 🖥️ Веб-интерфейс: **http://localhost:8501**
- 📡 API (Swagger): **http://localhost:8000/docs**

---

## 🧪 Пример входного файла (`test.csv`)
```csv
ID,text,src
174475,"Наблюдал веселую картину...",geo
86578,"После просмотра анимэ...",anime
```

## 📤 Пример выходного файла (`predictions.csv`)
```csv
ID,text,src,label,confidence
174475,"Наблюдал веселую картину...",geo,0,0.77
86578,"После просмотра анимэ...",anime,1,0.49
```

